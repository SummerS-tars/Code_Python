# 图像数据集整理脚本使用说明

## 🚀 快速开始

### 1. 准备数据
确保您的数据目录结构如下：
```
your_dataset/
├── pic/
│   ├── 365Scores_2024_2_5_16_7-8_train_1_1.png
│   ├── 365Scores_2024_2_5_16_7-8_val_2_1.png
│   └── ... (更多PNG文件)
└── txt/
    ├── 365Scores_2024_2_5_16_7-8.txt
    └── ... (更多TXT文件)
```

### 2. 运行脚本
```bash
python SortAndRename.py
```

### 3. 输入路径
程序会提示输入数据集根目录路径，例如：
```
请输入数据集根目录路径: C:\your_dataset
```

### 4. 确认处理
程序会显示即将进行的操作，确认后开始处理。

## 📊 处理结果

处理完成后，目录结构变为：
```
your_dataset/
├── train/
│   ├── 365Scores_1_1.png
│   ├── 365Scores_1_1.txt
│   └── ... (训练集文件)
├── val/
│   ├── 365Scores_2_1.png
│   ├── 365Scores_2_1.txt
│   └── ... (验证集文件)
├── error/
│   ├── pic/    (异常PNG文件)
│   └── txt/    (异常TXT文件)
├── pic/        (原目录，处理后为空)
└── txt/        (原目录，处理后为空)
```

## 🔧 高级用法

### 直接在代码中使用
```python
from SortAndRename import DatasetProcessor

# 创建处理器实例
processor = DatasetProcessor("path/to/your/dataset")

# 处理数据集
processor.process_dataset()
```

### 批处理多个数据集
```python
datasets = ["dataset1", "dataset2", "dataset3"]

for dataset_path in datasets:
    print(f"处理数据集: {dataset_path}")
    processor = DatasetProcessor(dataset_path)
    processor.process_dataset()
    print("-" * 50)
```

## ⚠️ 注意事项

1. **备份数据**: 建议在处理前备份原始数据
2. **文件命名**: 确保文件严格按照命名规则
3. **权限问题**: 确保对目标目录有读写权限
4. **大数据集**: 处理大型数据集时请耐心等待

## 🛠️ 故障排除

### 常见问题
- **文件名不匹配**: 检查是否严格按照 `tag_time-tag_train/val_class_duplicate.png` 格式
- **TXT文件找不到**: 确保TXT文件格式为 `tag_time-tag.txt`
- **权限错误**: 以管理员身份运行或检查目录权限

### 错误文件处理
所有无法正常处理的文件会被移动到 `error/` 目录中，您可以手动检查和处理这些文件。